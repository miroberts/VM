Run VM.exe

Commands:
	Load <nameofprogram>.osx
	Run - I havn't set it up to need the program name yet being able to only run one at a time for now.
	Coredump - I havn't set it up to need the program name yet being able to only run one at a time for now.
	Errordump - I havn't set it up to need the program name yet being able to only run one at a time for now.
	Test <nameofprogram>.osx -if no file name is given it defaults to Test.osx file
	Help - this will print out this list.
	Quit